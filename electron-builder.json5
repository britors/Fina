{
  appId: "com.britors.fina",
  productName: "Fina",
  copyright: "Copyright © 2026 Rodrigo Brito",

  directories: {
    output: "dist",
    buildResources: "build"
  },

  // Arquivos incluídos no pacote
  files: [
    "out/**/*",
    "package.json"
  ],

  // Rebuild de módulos nativos (better-sqlite3) para o Electron alvo
  npmRebuild: true,

  linux: {
    target: ["deb", "rpm"],
    category: "Office",
    synopsis: "Gerenciador de finanças pessoais",
    description: "Controle total das suas finanças: contas, transações, orçamentos, investimentos, metas e muito mais.",
    // build/icon.svg é convertido automaticamente pelo electron-builder no Linux
    icon: "build/icon.svg"
  },

  deb: {
    depends: ["libsecret-1-0", "libgtk-3-0", "libnotify4", "libnss3", "libxtst6"]
  },

  rpm: {
    depends: ["libsecret", "gtk3", "libnotify", "nss", "libXtst"]
  },

  win: {
    target: [{ target: "nsis", arch: ["x64"] }],
    // Gere build/icon.ico a partir de build/icon.svg:
    //   inkscape build/icon.svg -w 256 -h 256 -o build/icon.png
    //   convert build/icon.png -define icon:auto-resize=256,128,64,48,32,16 build/icon.ico
    icon: "build/icon.ico"
  },

  nsis: {
    oneClick: false,
    allowToChangeInstallationDirectory: true,
    createDesktopShortcut: true,
    createStartMenuShortcut: true,
    shortcutName: "Fina"
  }
}
