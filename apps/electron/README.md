# Fina Electron

Versão completa e multiplataforma do Fina, construída com Electron,
TypeScript e SQLite.

Os comandos podem ser executados na raiz do repositório:

```bash
npm install
npm run build:electron
npm run start:electron
```

Ou diretamente neste diretório com `npm run build`, `npm start` e `npm test`.
